import { Routes } from '@angular/router';
import { SubordinateRegisterComponent } from './register/register.component';

export const routes: Routes = [{ path: '', component: SubordinateRegisterComponent }
];
