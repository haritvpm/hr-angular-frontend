import { Routes } from '@angular/router';
import { SettingsEmployeeComponent } from './employee/employee.component';

export const routes: Routes = [{ path: 'employee', component: SettingsEmployeeComponent }
];
