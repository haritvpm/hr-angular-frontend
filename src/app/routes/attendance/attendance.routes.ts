import { Routes } from '@angular/router';
import { AttendanceRegisterComponent } from './register/register.component';

export const routes: Routes = [{ path: '', component: AttendanceRegisterComponent }
];
