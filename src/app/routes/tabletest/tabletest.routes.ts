import { Routes } from '@angular/router';
import { TabletestTableapiComponent } from './tableapi/tableapi.component';

export const routes: Routes = [{ path: '', component: TabletestTableapiComponent }
];
