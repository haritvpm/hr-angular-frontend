import { Routes } from '@angular/router';
import { AttendenceRegisterComponent } from './register/register.component';

export const routes: Routes = [{ path: '', component: AttendenceRegisterComponent }
];
