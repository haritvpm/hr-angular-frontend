import { Routes } from '@angular/router';
import { SampleApisampleComponent } from './apisample/apisample.component';

export const routes: Routes = [{ path: '', component: SampleApisampleComponent }
];
