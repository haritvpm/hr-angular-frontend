import { Routes } from '@angular/router';
import { LeavesApproveComponent } from './approve/approve.component';

export const routes: Routes = [{ path: 'approve', component: LeavesApproveComponent }
];
